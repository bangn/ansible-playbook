---

- hosts: "localhost"
  roles:
    - role: apt
      become: true
      become_user: root

    - role: users

    - role: thinkpad-tweak
      become: yes
      become_method: sudo

    - role: packages

    - role: dotfiles

    - role: cimon-io.asdf
      asdf_user: bangn
