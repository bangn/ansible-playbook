---

- hosts: "localhost"
  roles:
    - role: apt

    - role: users

    - role: packages

    - role: dotfiles

    - role: thinkpad-tweak
      become: yes
      become_method: sudo

    - role: cimon-io.asdf
      asdf_user: bangn
